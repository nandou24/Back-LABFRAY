# Puerto del servidor (Railway lo asigna automáticamente)
PORT=4000

# Conexión a MongoDB Atlas
BD_CNN=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=cluster

# Clave secreta para JWT
SECRET_JWT_SEED=your_secret_jwt_key_here

# Entorno de ejecución
NODE_ENV=production
